[project]
name = "pse-inline"
version = "0.1.0"
description = "Queue management app"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.139.0",
    "psycopg[binary]>=3.3.4",
]
